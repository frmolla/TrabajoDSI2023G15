<Page
    x:Class="TrabajoDSI2023G15.Collection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TrabajoDSI2023G15"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="0,0,-443,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1300"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.Background>
            <ImageBrush ImageSource="/Assets/background.png" Stretch="Fill"/>
        </Grid.Background>

        <StackPanel>
            <Canvas>
                <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
                    <AppBarButton Icon="Add" Label="Add"/>
                    <AppBarButton Icon="Edit" Label="Edit"/>
                    <AppBarButton Icon="Share" Label="Share"/>
                    <CommandBar.SecondaryCommands>
                        <AppBarButton Icon="Setting" Label="Settings">
                            <AppBarButton.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="Control" Key="I" />
                            </AppBarButton.KeyboardAccelerators>
                        </AppBarButton>
                    </CommandBar.SecondaryCommands>
                </CommandBar>
                <Grid Height="998" Width="1300" HorizontalAlignment="Center" VerticalAlignment="Top">
                    <Rectangle Margin="40"
                    Fill="#d5403f"
                    Opacity="0.8"
                    StrokeThickness="8"
                    Stroke="#f4e86a"/>
                </Grid>
                <Rectangle Width="484" Height="98" 
                VerticalAlignment="Top" 
                HorizontalAlignment="Left" 
                Fill="#d5403f" 
                Margin="90"
                Opacity="0.8"
                StrokeThickness="8"
                Stroke="#f4e86a"/>
                <TextBlock Text="COLLECTION" FontSize="40" Foreground="#f4e86a" FontFamily="Quando" FontWeight="Bold" Canvas.Left="212" Canvas.Top="116" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Image Source="/Assets/card_icon(1).png" Height="75" Margin="114,102,1308,823"/>
            </Canvas>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <Canvas>
                <Grid Height="998" Width="637" HorizontalAlignment="Left" VerticalAlignment="Center" Canvas.Left="-6">
                    <Rectangle Margin="40"
                    Fill="#d5403f"
                    Opacity="0.8"
                    StrokeThickness="8"
                    Stroke="#f4e86a" Canvas.Left="33" Canvas.Top="46"/>
                </Grid>
                <Rectangle Width="454" Height="90" 
                VerticalAlignment="Top" 
                HorizontalAlignment="Left" 
                Fill="#d5403f" 
                Margin="90"
                Opacity="0.8"
                StrokeThickness="8"
                Stroke="#f4e86a"/>
                <TextBlock Text="DECK" FontSize="40" Foreground="#f4e86a" FontFamily="Quando" FontWeight="Bold" Canvas.Left="244" Canvas.Top="111" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <Image Source="/Assets/card_icon(1).png" Height="75" Canvas.Left="114" Canvas.Top="98"/>
            </Canvas>
        </StackPanel>


        <Button x:Name="BackButton"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        IsEnabled="{x:Bind Frame.CanGoBack, Mode=OneWay}" 
                        ToolTipService.ToolTip="Back" 
                        VerticalAlignment="Bottom"
                        Margin="97,0,0,86"
                        Foreground="#f4e86a"
                        FontSize="100"
                        Click="BackButton_OnClick"/>
    </Grid>
</Page>
